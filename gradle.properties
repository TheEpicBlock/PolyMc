# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G

# Fabric Properties
	# check these on https://fabricmc.net/develop/
	# note that we're using quilt-loader instead of fabric (so we can compile against both fapi and qsl)
	minecraft_version=1.20.4
	yarn_mappings=1.20.4+build.1
	loader_version=0.22.1-beta.4

# Mod Properties
	mod_version = 5.6.1
	maven_group = nl.theepicblock
	archives_base_name = PolyMc

# Dependencies
	# currently not on the main fabric site, check on the maven: https://maven.fabricmc.net/net/fabricmc/fabric-api/fabric-api
	fabric_version=0.91.1+1.20.4
	qsl_version=6.1.2+1.20.1
	lithium_version=mc1.19.4-0.11.1
	immersive_portals_version=v4.0.1-mc1.20.2
	resource_locator_api_version=0.5.1+1.20.1
	packet_tweaker_version=0.5.0+1.20.2-rc1
